diff --git "a/c:\\sw\\XM62022Bueno\\vm\\memory.cpp" "b/c:\\sw\\XM62022\\vm\\memory.cpp"
index a893463..a6c7723 100644
--- "a/c:\\sw\\XM62022Bueno\\vm\\memory.cpp"
+++ "b/c:\\sw\\XM62022\\vm\\memory.cpp"
@@ -2,8 +2,8 @@
 //
 //	X68000 EMULATOR "XM6"
 //
-//	Copyright (C) 2001-2006 ?o?h?D(ytanaka@ipc-tokai.or.jp)
-//	[ ?????? ]
+//	Copyright (C) 2001-2006 ??????????????????ytanaka@ipc-tokai.or.jp)
+//	[ ?????????????????? ]
 //
 //---------------------------------------------------------------------------
 
@@ -24,21 +24,22 @@
 
 //---------------------------------------------------------------------------
 //
-//	?X?^?e?B?b?N ???[?N
+//	?????????????????????????????? ??????????????????
 //
 //---------------------------------------------------------------------------
 static CPU *pCPU;
+static Memory *pMemory;
 
 //---------------------------------------------------------------------------
 //
-//	?o?X?G???[??????(???C???????????????G???A????)
+//	??????????????????????????????????????????(?????????????????????????????????????????????????????????????????????????????????)
 //
 //---------------------------------------------------------------------------
 extern "C" {
 
 //---------------------------------------------------------------------------
 //
-//	?????????o?X?G???[
+//	???????????????????????????????????????????????????
 //
 //---------------------------------------------------------------------------
 void ReadBusErr(DWORD addr)
@@ -48,7 +49,7 @@ void ReadBusErr(DWORD addr)
 
 //---------------------------------------------------------------------------
 //
-//	?????????o?X?G???[
+//	???????????????????????????????????????????????????
 //
 //---------------------------------------------------------------------------
 void WriteBusErr(DWORD addr)
@@ -59,67 +60,67 @@ void WriteBusErr(DWORD addr)
 
 //===========================================================================
 //
-//	??????
+//	??????????????????
 //
 //===========================================================================
 
 //---------------------------------------------------------------------------
 //
-//	?R???X?g???N?^
+//	??????????????????????????????????????
 //
 //---------------------------------------------------------------------------
 Memory::Memory(VM *p) : MemDevice(p)
 {
-	// ?f?o?C?XID????????
+	// ?????????????????????ID??????????????????
 	dev.id = MAKEID('M', 'E', 'M', ' ');
 	dev.desc = "Memory Ctrl (OHM2)";
 
-	// ?J?n?A?h???X?A?I???A?h???X
+	// ????????????????????????????????????????????????????????????????
 	memdev.first = 0;
 	memdev.last = 0xffffff;
 
-	// RAM/ROM?o?b?t?@
+	// RAM/ROM??????????????????
 	mem.ram = NULL;
 	mem.ipl = NULL;
 	mem.cg = NULL;
 	mem.scsi = NULL;
 
-	// RAM??2MB
+	// RAM??????2MB
 	mem.size = 2;
 	mem.config = 0;
 	mem.length = 0;
 
-	// ???????^?C?v???????[?h
+	// ???????????????????????????????????????????????????????????????
 	mem.type = None;
 	mem.now = None;
 
-	// ?I?u?W?F?N?g
+	// ?????????????????????????????????
 	areaset = NULL;
 	sram = NULL;
 
-	// ??????
+	// ???????????????
 	memset(mem.table, 0, sizeof(mem.table));
 	mem.memsw = TRUE;
 
-	// static???[?N
+	// static??????????????????
 	::pCPU = NULL;
 }
 
 //---------------------------------------------------------------------------
 //
-//	??????
+//	???????????????
 //
 //---------------------------------------------------------------------------
 BOOL FASTCALL Memory::Init()
 {
 	ASSERT(this);
 
-	// ???{?N???X
+	// ??????????????????????????????
 	if (!MemDevice::Init()) {
 		return FALSE;
 	}
 
-	// ???C????????
+	// ????????????????????????????????????
 	mem.length = mem.size * 0x100000;
 	try {
 		mem.ram = new BYTE[ mem.length ];
@@ -131,7 +132,7 @@ BOOL FASTCALL Memory::Init()
 		return FALSE;
 	}
 
-	// ???C???????????[???N???A????
+	// ??????????????????????????????????????????????????????????????????????????????
 	memset(mem.ram, 0x00, mem.length);
 
 	// IPL ROM
@@ -167,13 +168,13 @@ BOOL FASTCALL Memory::Init()
 		return FALSE;
 	}
 
-	// SASI??ROM???K?{???????A???????[?h????
+	// SASI??????ROM???????????????????????????????????????????????????????????????????????????
 	if (!LoadROM(SASI)) {
-		// IPLROM.DAT, CGROM.DAT?????????????p?^?[??
+		// IPLROM.DAT, CGROM.DAT???????????????????????????????????????????????????
 		return FALSE;
 	}
 
-	// ????ROM?????????AXVI??Compact??030???????A?????????????????????D??????
+	// ?????????ROM????????????????????????VI??????ompact??????30???????????????????????????????????????????????????????????????????????????????????????
 	if (LoadROM(XVI)) {
 		mem.now = XVI;
 	}
@@ -188,36 +189,40 @@ BOOL FASTCALL Memory::Init()
 		}
 	}
 
-	// XVI,Compact,030???????????????????????A???xSASI??????
+	// XVI,Compact,030??????????????????????????????????????????????????????????????????SASI?????????????????
 	if (mem.type == None) {
 		LoadROM(SASI);
 		mem.now = SASI;
 	}
 
-	// ???[?W?????G???A??????
-	::s68000context.u_fetch = u_pgr;
-	::s68000context.s_fetch = s_pgr;
-	::s68000context.u_readbyte = u_rbr;
-	::s68000context.s_readbyte = s_rbr;
-	::s68000context.u_readword = u_rwr;
-	::s68000context.s_readword = s_rwr;
-	::s68000context.u_writebyte = u_wbr;
-	::s68000context.s_writebyte = s_wbr;
-	::s68000context.u_writeword = u_wwr;
-	::s68000context.s_writeword = s_wwr;
-
-	// ?G???A?Z?b?g????
+	// ??????????????????????????????????????????????????????????????????
+	// NOTE: Musashi doesn't use Starscream-style memory regions.
+	// Memory access is handled via m68k_read/write_memory callbacks.
+	// Region pointers are kept in s68000context as stubs for compilation.
+	// ::s68000context.u_fetch = u_pgr;
+	// ::s68000context.s_fetch = s_pgr;
+	// ::s68000context.u_readbyte = u_rbr;
+	// ::s68000context.s_readbyte = s_rbr;
+	// ::s68000context.u_readword = u_rwr;
+	// ::s68000context.s_readword = s_rwr;
+	// ::s68000context.u_writebyte = u_wbr;
+	// ::s68000context.s_writebyte = s_wbr;
+	// ::s68000context.u_writeword = u_wwr;
+	// ::s68000context.s_writeword = s_wwr;
+
+	// ?????????????????????????????????????????????
 	areaset = (AreaSet*)vm->SearchDevice(MAKEID('A', 'R', 'E', 'A'));
 	ASSERT(areaset);
 
-	// SRAM????
+	// SRAM????????????
 	sram = (SRAM*)vm->SearchDevice(MAKEID('S', 'R', 'A', 'M'));
 	ASSERT(sram);
 
-	// static???[?N
+	// static??????????????????
 	::pCPU = cpu;
+	::pMemory = this;
 
-	// ???????e?[?u??????
+	// ????????????????????????????????????????????
 	InitTable();
 
 	return TRUE;
@@ -225,7 +230,7 @@ BOOL FASTCALL Memory::Init()
 
 //---------------------------------------------------------------------------
 //
-//	ROM???[?h
+//	ROM??????????????????
 //
 //---------------------------------------------------------------------------
 BOOL FASTCALL Memory::LoadROM(memtype target)
@@ -240,7 +245,7 @@ BOOL FASTCALL Memory::LoadROM(memtype target)
 
 	ASSERT(this);
 
-	// ???U????????ROM?G???A?????????ANone??
+	// ????????????????????????????????ROM???????????????????????????????????????????one??????
 	memset(mem.ipl, 0xff, 0x20000);
 	memset(mem.cg, 0xff, 0xc0000);
 	memset(mem.scsi, 0xff, 0x20000);
@@ -270,7 +275,7 @@ BOOL FASTCALL Memory::LoadROM(memtype target)
 		return FALSE;
 	}
 
-	// IPL?o?C?g?X???b?v
+	// IPL?????????????????????????????????
 	ptr = mem.ipl;
 	for (i=0; i<0x10000; i++) {
 		data = ptr[0];
@@ -282,14 +287,14 @@ BOOL FASTCALL Memory::LoadROM(memtype target)
 	// CG
 	path.SysFile(Filepath::CG);
 	if (!fio.Load(path, mem.cg, 0xc0000)) {
-		// ?t?@?C?????????????ACGTMP?????g???C
+		// ???????????????????????????????????????????????????GTMP??????????????????????????
 		path.SysFile(Filepath::CGTMP);
 		if (!fio.Load(path, mem.cg, 0xc0000)) {
 			return FALSE;
 		}
 	}
 
-	// CG?o?C?g?X???b?v
+	// CG?????????????????????????????????
 	ptr = mem.cg;
 	for (i=0; i<0x60000; i++) {
 		data = ptr[0];
@@ -301,7 +306,7 @@ BOOL FASTCALL Memory::LoadROM(memtype target)
 	// SCSI
 	scsi_req = FALSE;
 	switch (target) {
-		// ????
+		// ?????????
 		case SCSIInt:
 		case XVI:
 		case Compact:
@@ -312,21 +317,21 @@ BOOL FASTCALL Memory::LoadROM(memtype target)
 			path.SysFile(Filepath::ROM030);
 			scsi_req = TRUE;
 			break;
-		// ?O?t
+		// ????????????
 		case SCSIExt:
 			path.SysFile(Filepath::SCSIExt);
 			scsi_req = TRUE;
 			break;
-		// SASI(ROM?K?v????)
+		// SASI(ROM?????????????????????
 		case SASI:
 			break;
-		// ??????(??????????)
+		// ???????????????????????????????????????
 		default:
 			ASSERT(FALSE);
 			break;
 	}
 	if (scsi_req) {
-		// X68030????ROM30.DAT(0x20000?o?C?g)?A????????0x2000?o?C?g???g???C
+		// X68030????????????ROM30.DAT(0x20000???????????????????????????????????????0x2000????????????????????????????????
 		if (target == X68030) {
 			scsi_size = 0x20000;
 		}
@@ -334,17 +339,17 @@ BOOL FASTCALL Memory::LoadROM(memtype target)
 			scsi_size = 0x2000;
 		}
 
-		// ?????|?C???^??????
+		// ????????????????????????????????????????????????
 		ptr = mem.scsi;
 
-		// ???[?h
+		// ??????????????????
 		if (!fio.Load(path, mem.scsi, scsi_size)) {
-			// SCSIExt??0x1fe0?o?C?g??????(WinX68k??????????????????)
+			// SCSIExt??????0x1fe0??????????????????????????????WinX68k????????????????????????????????????????????
 			if (target != SCSIExt) {
 				return FALSE;
 			}
 
-			// 0x1fe0?o?C?g?????g???C
+			// 0x1fe0???????????????????????????????????????
 			scsi_size = 0x1fe0;
 			ptr = &mem.scsi[0x20];
 			if (!fio.Load(path, &mem.scsi[0x0020], scsi_size)) {
@@ -352,7 +357,7 @@ BOOL FASTCALL Memory::LoadROM(memtype target)
 			}
 		}
 
-		// SCSI?o?C?g?X???b?v
+		// SCSI?????????????????????????????????
 		for (i=0; i<scsi_size; i+=2) {
 			data = ptr[0];
 			ptr[0] = ptr[1];
@@ -361,15 +366,15 @@ BOOL FASTCALL Memory::LoadROM(memtype target)
 		}
 	}
 
-	// ?^?[?Q?b?g???J?????g???Z?b?g?????A????
+	// ????????????????????????????????????????????????????????????????????????????????????????????????
 	mem.type = target;
 	return TRUE;
 }
 
 //---------------------------------------------------------------------------
 //
-//	???????e?[?u??????
-//	?????????f?R?[?_??????
+//	?????????????????????????????????????????
+//	??????????????????????????????????????????????????????????????
 //
 //---------------------------------------------------------------------------
 void FASTCALL Memory::InitTable()
@@ -389,44 +394,44 @@ void FASTCALL Memory::InitTable()
 
 	ASSERT(this);
 
-	// ?|?C???^??????
+	// ????????????????????????????????????
 	mdev = this;
 	i = 0;
 
-	// Memory???~???f?o?C?X?????????A?|?C???^???z??????????
+	// Memory????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????
 	while (mdev) {
 		devarray[i] = mdev;
 
-		// ????
+		// ????????????
 		i++;
 		mdev = (MemDevice*)mdev->GetNextDevice();
 	}
 
-	// ?A?Z???u?????[?`?????????o???A?e?[?u?????????n??
+	// ??????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????
 	MemInitDecode(this, devarray);
 
-	// ?A?Z???u?????[?`?????o?????e?[?u?????t??????(?A???C???????g??????)
+	// ??????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????
 	table = MemDecodeTable;
 	for (i=0; i<0x180; i++) {
-		// 4?o?C?g??????DWORD?l???????????A?|?C???^???L???X?g
+		// 4??????????????????????????????DWORD??????????????????????????????????????????????????????????????????????????????????????????
 		ptr = *(DWORD*)table;
 		mem.table[i] = (MemDevice*)ptr;
 
-		// ????
+		// ????????????
 		table += 4;
 	}
 }
 
 //---------------------------------------------------------------------------
 //
-//	?N???[???A?b?v
+//	???????????????????????????????????????
 //
 //---------------------------------------------------------------------------
 void FASTCALL Memory::Cleanup()
 {
 	ASSERT(this);
 
-	// ??????????
+	// ??????????????????????????????
 	if (mem.ram) {
 		delete[] mem.ram;
 		mem.ram = NULL;
@@ -444,13 +449,13 @@ void FASTCALL Memory::Cleanup()
 		mem.scsi = NULL;
 	}
 
-	// ???{?N???X??
+	// ????????????????????????????????????
 	MemDevice::Cleanup();
 }
 
 //---------------------------------------------------------------------------
 //
-//	???Z?b?g
+//	?????????????????????
 //
 //---------------------------------------------------------------------------
 void FASTCALL Memory::Reset()
@@ -458,30 +463,30 @@ void FASTCALL Memory::Reset()
 	int size;
 
 	ASSERT(this);
-	LOG0(Log::Normal, "???Z?b?g");
+	LOG0(Log::Normal, "(message)");
 
-	// ???????^?C?v?????v??????????
+	// ??????????????????????????????????????????????????????????????????????????
 	if (mem.type != mem.now) {
 		if (LoadROM(mem.type)) {
-			// ROM???????????????B???[?h??????
+			// ROM?????????????????????????????????????????????????????????????????
 			mem.now = mem.type;
 		}
 		else {
-			// ROM?????????????BSASI?^?C?v???????A??????SASI??????
+			// ROM???????????????????????????????????ASI???????????????????????????????????????????????????SASI??????????????????
 			LoadROM(SASI);
 			mem.now = SASI;
 			mem.type = SASI;
 		}
 
-		// ?R???e?L?X?g??????????(CPU::Reset???????????????????A?K??FALSE)
+		// ?????????????????????????????????????????????????????????CPU::Reset????????????????????????????????????????????????????????????FALSE)
 		MakeContext(FALSE);
 	}
 
-	// ???????T?C?Y?????v??????????
+	// ?????????????????????????????????????????????????????????????????????????????
 	if (mem.size == ((mem.config + 1) * 2)) {
-		// ???v?????????????A???????X?C?b?`?????X?V?`?F?b?N
+		// ?????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????
 		if (mem.memsw) {
-			// $ED0008 : ???C??RAM?T?C?Y
+			// $ED0008 : ??????????????????RAM??????????????????
 			size = mem.size << 4;
 			sram->SetMemSw(0x08, 0x00);
 			sram->SetMemSw(0x09, size);
@@ -491,10 +496,10 @@ void FASTCALL Memory::Reset()
 		return;
 	}
 
-	// ???X
+	// ?????????
 	mem.size = (mem.config + 1) * 2;
 
-	// ???m??
+	// ??????????????????
 	ASSERT(mem.ram);
 	delete[] mem.ram;
 	mem.ram = NULL;
@@ -503,31 +508,31 @@ void FASTCALL Memory::Reset()
 		mem.ram = new BYTE[ mem.length ];
 	}
 	catch (...) {
-		// ???????s??????????2MB??????
+		// ???????????????????????????????????????????????????2MB??????????????????
 		mem.config = 0;
 		mem.size = 2;
 		mem.length = mem.size * 0x100000;
 		mem.ram = new BYTE[ mem.length ];
 	}
 	if (!mem.ram) {
-		// ???????s??????????2MB??????
+		// ???????????????????????????????????????????????????2MB??????????????????
 		mem.config = 0;
 		mem.size = 2;
 		mem.length = mem.size * 0x100000;
 		mem.ram = new BYTE[ mem.length ];
 	}
 
-	// ?????????m????????????????????
+	// ??????????????????????????????????????????????????????????????????????????????
 	if (mem.ram) {
 		memset(mem.ram, 0x00, mem.length);
 
-		// ?R???e?L?X?g??????????(CPU::Reset???????????????????A?K??FALSE)
+		// ?????????????????????????????????????????????????????????CPU::Reset????????????????????????????????????????????????????????????FALSE)
 		MakeContext(FALSE);
 	}
 
-	// ???????X?C?b?`?????X?V
+	// ????????????????????????????????????????????????????????????
 	if (mem.memsw) {
-		// $ED0008 : ???C??RAM?T?C?Y
+		// $ED0008 : ??????????????????RAM??????????????????
 		size = mem.size << 4;
 		sram->SetMemSw(0x08, 0x00);
 		sram->SetMemSw(0x09, size);
@@ -538,32 +543,32 @@ void FASTCALL Memory::Reset()
 
 //---------------------------------------------------------------------------
 //
-//	?Z?[?u
+//	??????????????????
 //
 //---------------------------------------------------------------------------
 BOOL FASTCALL Memory::Save(Fileio *fio, int /*ver*/)
 {
 	ASSERT(this);
-	LOG0(Log::Normal, "?Z?[?u");
 
-	// ?^?C?v??????
+
+	// ?????????????????????????????????
 	if (!fio->Write(&mem.now, sizeof(mem.now))) {
 		return FALSE;
 	}
 
-	// SCSI ROM?????e?????? (X68030???O)
+	// SCSI ROM?????????????????????????????????(X68030????????????
 	if (mem.now != X68030) {
 		if (!fio->Write(mem.scsi, 0x2000)) {
 			return FALSE;
 		}
 	}
 
-	// mem.size??????
+	// mem.size???????????????
 	if (!fio->Write(&mem.size, sizeof(mem.size))) {
 		return FALSE;
 	}
 
-	// ????????????
+	// ?????????????????????????????????
 	if (!fio->Write(mem.ram, mem.length)) {
 		return FALSE;
 	}
@@ -573,7 +578,7 @@ BOOL FASTCALL Memory::Save(Fileio *fio, int /*ver*/)
 
 //---------------------------------------------------------------------------
 //
-//	???[?h
+//	??????????????????
 //
 //---------------------------------------------------------------------------
 BOOL FASTCALL Memory::Load(Fileio *fio, int /*ver*/)
@@ -582,48 +587,48 @@ BOOL FASTCALL Memory::Load(Fileio *fio, int /*ver*/)
 	BOOL context;
 
 	ASSERT(this);
-	LOG0(Log::Normal, "???[?h");
+	LOG0(Log::Normal, "Load");
 
-	// ?R???e?L?X?g??????????????
+	// ??????????????????????????????????????????????????????????????????
 	context = FALSE;
 
-	// ?^?C?v??????
+	// ????????????????????????????????
 	if (!fio->Read(&mem.type, sizeof(mem.type))) {
 		return FALSE;
 	}
 
-	// ?^?C?v??????????????????????????
+	// ????????????????????????????????????????????????????????????????????????????????????
 	if (mem.type != mem.now) {
-		// ROM??????????
+		// ROM??????????????????????????????
 		if (!LoadROM(mem.type)) {
-			// ?Z?[?u????????????????ROM???A??????????????
+			// ??????????????????????????????????????????????????????ROM????????????????????????????????????????????
 			LoadROM(mem.now);
 			return FALSE;
 		}
 
-		// ROM????????????????????
+		// ROM???????????????????????????????????????????????????
 		mem.now = mem.type;
 		context = TRUE;
 	}
 
-	// SCSI ROM?????e?????? (X68030???O)
+	// SCSI ROM??????????????????????????????????? (X68030????????????
 	if (mem.type != X68030) {
 		if (!fio->Read(mem.scsi, 0x2000)) {
 			return FALSE;
 		}
 	}
 
-	// mem.size??????
+	// mem.size?????????????????
 	if (!fio->Read(&size, sizeof(size))) {
 		return FALSE;
 	}
 
-	// mem.size?????v??????????????
+	// mem.size??????????????????????????????????????????????????
 	if (mem.size != size) {
-		// ???X????
+		// ??????????????????
 		mem.size = size;
 
-		// ???m??
+		// ??????????????????
 		delete[] mem.ram;
 		mem.ram = NULL;
 		mem.length = mem.size * 0x100000;
@@ -634,26 +639,26 @@ BOOL FASTCALL Memory::Load(Fileio *fio, int /*ver*/)
 			mem.ram = NULL;
 		}
 		if (!mem.ram) {
-			// ???????s??????????2MB??????
+			// ???????????????????????????????????????????????????2MB??????????????????
 			mem.config = 0;
 			mem.size = 2;
 			mem.length = mem.size * 0x100000;
 			mem.ram = new BYTE[ mem.length ];
 
-			// ???[?h???s
+			// ??????????????????????????????
 			return FALSE;
 		}
 
-		// ?R???e?L?X?g?????????K?v
+		// ???????????????????????????????????????????????????????????????
 		context = TRUE;
 	}
 
-	// ????????????
+	// ???????????????????????????????????
 	if (!fio->Read(mem.ram, mem.length)) {
 		return FALSE;
 	}
 
-	// ?K?v?????????A?R???e?L?X?g??????????
+	// ??????????????????????????????????????????????????????????????????????????????????????????
 	if (context) {
 		MakeContext(FALSE);
 	}
@@ -663,29 +668,29 @@ BOOL FASTCALL Memory::Load(Fileio *fio, int /*ver*/)
 
 //---------------------------------------------------------------------------
 //
-//	?????K?p
+//	?????????????????????
 //
 //---------------------------------------------------------------------------
 void FASTCALL Memory::ApplyCfg(const Config *config)
 {
 	ASSERT(this);
 	ASSERT(config);
-	LOG0(Log::Normal, "?????K?p");
+	LOG0(Log::Normal, "?????????????????????");
 
-	// ??????????(ROM???[?h?????????Z?b?g??)
+	// ??????????????????????????????(ROM????????????????????????????????????????????????????????
 	mem.type = (memtype)config->mem_type;
 
-	// RAM?T?C?Y(???????m???????????Z?b?g??)
+	// RAM??????????????????(????????????????????????????????????????????????????????????????????
 	mem.config = config->ram_size;
 	ASSERT((mem.config >= 0) && (mem.config <= 5));
 
-	// ???????X?C?b?`?????X?V
+	// ????????????????????????????????????????????????????????????
 	mem.memsw = config->ram_sramsync;
 }
 
 //---------------------------------------------------------------------------
 //
-//	?o?C?g????????
+//	???????????????????????????????????????
 //
 //---------------------------------------------------------------------------
 DWORD FASTCALL Memory::ReadByte(DWORD addr)
@@ -696,7 +701,12 @@ DWORD FASTCALL Memory::ReadByte(DWORD addr)
 	ASSERT(addr <= 0xffffff);
 	ASSERT(mem.now != None);
 
-	// ???C??RAM
+	// IPL ROM shadowed at 0x000000 during boot (AreaSet == 0)
+	if (addr < 0x20000 && areaset && areaset->GetArea() == 0) {
+		return (DWORD)mem.ipl[addr ^ 1];
+	}
+
+	// ??????????????????RAM
 	if (addr < mem.length) {
 		return (DWORD)mem.ram[addr ^ 1];
 	}
@@ -708,30 +718,30 @@ DWORD FASTCALL Memory::ReadByte(DWORD addr)
 		return (DWORD)mem.ipl[addr];
 	}
 
-	// IPL?C???[?W or SCSI????
+	// IPL???????????????????????? or SCSI?????????
 	if (addr >= 0xfc0000) {
-		// IPL?C???[?W??
+		// IPL??????????????????????????????
 		if ((mem.now == SASI) || (mem.now == SCSIExt)) {
-			// IPL?C???[?W
+			// IPL????????????????????????
 			addr &= 0x1ffff;
 			addr ^= 1;
 			return (DWORD)mem.ipl[addr];
 		}
-		// SCSI??????(?????`?F?b?N)
+		// SCSI??????????????????????????????????????????)
 		if (addr < 0xfc2000) {
-			// SCSI????
+			// SCSI?????????
 			addr &= 0x1fff;
 			addr ^= 1;
 			return (DWORD)mem.scsi[addr];
 		}
-		// X68030 IPL?O????
+		// X68030 IPL???????????????
 		if (mem.now == X68030) {
-			// X68030 IPL?O??
+			// X68030 IPL?????????
 			addr &= 0x1ffff;
 			addr ^= 1;
 			return (DWORD)mem.scsi[addr];
 		}
-		// SCSI???????f?????AROM?????O
+		// SCSI???????????????????????????????????OM???????????????
 		return 0xff;
 	}
 
@@ -742,7 +752,7 @@ DWORD FASTCALL Memory::ReadByte(DWORD addr)
 		return (DWORD)mem.cg[addr];
 	}
 
-	// SCSI?O?t
+	// SCSI????????????
 	if (mem.now == SCSIExt) {
 		if ((addr >= 0xea0020) && (addr <= 0xea1fff)) {
 			addr &= 0x1fff;
@@ -751,7 +761,7 @@ DWORD FASTCALL Memory::ReadByte(DWORD addr)
 		}
 	}
 
-	// ?f?o?C?X?f?B?X?p?b?`
+	// ???????????????????????????????????????????????????
 	if (addr >= 0xc00000) {
 		index = addr - 0xc00000;
 		index >>= 13;
@@ -761,14 +771,14 @@ DWORD FASTCALL Memory::ReadByte(DWORD addr)
 		}
 	}
 
-	LOG1(Log::Warning, "?????`?o?C?g???????? $%06X", addr);
+	LOG1(Log::Warning, "(message)", addr);
 	cpu->BusErr(addr, TRUE);
 	return 0xff;
 }
 
 //---------------------------------------------------------------------------
 //
-//	???[?h????????
+//	??????????????????????????????????????????
 //
 //---------------------------------------------------------------------------
 DWORD FASTCALL Memory::ReadWord(DWORD addr)
@@ -781,14 +791,21 @@ DWORD FASTCALL Memory::ReadWord(DWORD addr)
 	ASSERT(addr <= 0xffffff);
 	ASSERT(mem.now != None);
 
-	// CPU?????????????????????????????????ADMAC?????????????`?F?b?N?K?v????
+	// CPU?????????????????????????????????????????????????????????????????????????????????????????MAC?????????????????????????????????????????????????????????????????????
 	if (addr & 1) {
-		// ???UCPU???n??(CPU?o?R??DMA??)
+		// ???????????CPU??????????????????CPU???????????????DMA??????)
 		cpu->AddrErr(addr, TRUE);
 		return 0xffff;
 	}
 
-	// ???C??RAM
+	// IPL ROM shadowed at 0x000000 during boot (AreaSet == 0)
+	if (addr < 0x20000 && areaset && areaset->GetArea() == 0) {
+		ptr = (WORD*)(&mem.ipl[addr]);
+		data = (DWORD)*ptr;
+		return data;
+	}
+
+	// ??????????????????RAM
 	if (addr < mem.length) {
 		ptr = (WORD*)(&mem.ram[addr]);
 		data = (DWORD)*ptr;
@@ -803,33 +820,33 @@ DWORD FASTCALL Memory::ReadWord(DWORD addr)
 		return data;
 	}
 
-	// IPL?C???[?W or SCSI????
+	// IPL???????????????????????? or SCSI?????????
 	if (addr >= 0xfc0000) {
-		// IPL?C???[?W??
+		// IPL??????????????????????????????
 		if ((mem.now == SASI) || (mem.now == SCSIExt)) {
-			// IPL?C???[?W
+			// IPL????????????????????????
 			addr &= 0x1ffff;
 			ptr = (WORD*)(&mem.ipl[addr]);
 			data = (DWORD)*ptr;
 			return data;
 		}
-		// SCSI??????(?????`?F?b?N)
+		// SCSI??????????????????????????????????????????)
 		if (addr < 0xfc2000) {
-			// SCSI????
+			// SCSI?????????
 			addr &= 0x1fff;
 			ptr = (WORD*)(&mem.scsi[addr]);
 			data = (DWORD)*ptr;
 			return data;
 		}
-		// X68030 IPL?O????
+		// X68030 IPL???????????????
 		if (mem.now == X68030) {
-			// X68030 IPL?O??
+			// X68030 IPL?????????
 			addr &= 0x1ffff;
 			ptr = (WORD*)(&mem.scsi[addr]);
 			data = (DWORD)*ptr;
 			return data;
 		}
-		// SCSI???????f?????AROM?????O
+		// SCSI???????????????????????????????????OM???????????????
 		return 0xffff;
 	}
 
@@ -841,7 +858,7 @@ DWORD FASTCALL Memory::ReadWord(DWORD addr)
 		return data;
 	}
 
-	// SCSI?O?t
+	// SCSI????????????
 	if (mem.now == SCSIExt) {
 		if ((addr >= 0xea0020) && (addr <= 0xea1fff)) {
 			addr &= 0x1fff;
@@ -851,7 +868,7 @@ DWORD FASTCALL Memory::ReadWord(DWORD addr)
 		}
 	}
 
-	// ?f?o?C?X?f?B?X?p?b?`
+	// ???????????????????????????????????????????????????
 	if (addr >= 0xc00000) {
 		index = addr - 0xc00000;
 		index >>= 13;
@@ -861,15 +878,15 @@ DWORD FASTCALL Memory::ReadWord(DWORD addr)
 		}
 	}
 
-	// ?o?X?G???[
-	LOG1(Log::Warning, "?????`???[?h???????? $%06X", addr);
+	// ???????????????????????????
+	LOG1(Log::Warning, "(message)", addr);
 	cpu->BusErr(addr, TRUE);
 	return 0xffff;
 }
 
 //---------------------------------------------------------------------------
 //
-//	?o?C?g????????
+//	????????????????????????????????????
 //
 //---------------------------------------------------------------------------
 void FASTCALL Memory::WriteByte(DWORD addr, DWORD data)
@@ -881,7 +898,7 @@ void FASTCALL Memory::WriteByte(DWORD addr, DWORD data)
 	ASSERT(data < 0x100);
 	ASSERT(mem.now != None);
 
-	// ???C??RAM
+	// ??????????????????RAM
 	if (addr < mem.length) {
 		mem.ram[addr ^ 1] = (BYTE)data;
 		return;
@@ -892,7 +909,7 @@ void FASTCALL Memory::WriteByte(DWORD addr, DWORD data)
 		return;
 	}
 
-	// ?f?o?C?X?f?B?X?p?b?`
+	// ???????????????????????????????????????????????????
 	if (addr >= 0xc00000) {
 		index = addr - 0xc00000;
 		index >>= 13;
@@ -903,14 +920,14 @@ void FASTCALL Memory::WriteByte(DWORD addr, DWORD data)
 		}
 	}
 
-	// ?o?X?G???[
+	// ???????????????????????????
 	cpu->BusErr(addr, FALSE);
-	LOG2(Log::Warning, "?????`?o?C?g???????? $%06X <- $%02X", addr, data);
+	LOG2(Log::Warning, "(message)", addr, data);
 }
 
 //---------------------------------------------------------------------------
 //
-//	???[?h????????
+//	???????????????????????????????????????
 //
 //---------------------------------------------------------------------------
 void FASTCALL Memory::WriteWord(DWORD addr, DWORD data)
@@ -923,14 +940,14 @@ void FASTCALL Memory::WriteWord(DWORD addr, DWORD data)
 	ASSERT(data < 0x10000);
 	ASSERT(mem.now != None);
 
-	// CPU?????????????????????????????????ADMAC?????????????`?F?b?N?K?v????
+	// CPU?????????????????????????????????????????????????????????????????????????????????????????MAC?????????????????????????????????????????????????????????????????????
 	if (addr & 1) {
-		// ???UCPU???n??(CPU?o?R??DMA??)
+		// ???????????CPU??????????????????CPU???????????????DMA??????)
 		cpu->AddrErr(addr, FALSE);
 		return;
 	}
 
-	// ???C??RAM
+	// ??????????????????RAM
 	if (addr < mem.length) {
 		ptr = (WORD*)(&mem.ram[addr]);
 		*ptr = (WORD)data;
@@ -942,7 +959,7 @@ void FASTCALL Memory::WriteWord(DWORD addr, DWORD data)
 		return;
 	}
 
-	// ?f?o?C?X?f?B?X?p?b?`
+	// ???????????????????????????????????????????????????
 	if (addr >= 0xc00000) {
 		index = addr - 0xc00000;
 		index >>= 13;
@@ -953,14 +970,14 @@ void FASTCALL Memory::WriteWord(DWORD addr, DWORD data)
 		}
 	}
 
-	// ?o?X?G???[
+	// ???????????????????????????
 	cpu->BusErr(addr, FALSE);
-	LOG2(Log::Warning, "?????`???[?h???????? $%06X <- $%04X", addr, data);
+	LOG2(Log::Warning, "(message)", addr, data);
 }
 
 //---------------------------------------------------------------------------
 //
-//	????????????
+//	????????????????????????????????????
 //
 //---------------------------------------------------------------------------
 DWORD FASTCALL Memory::ReadOnly(DWORD addr) const
@@ -971,7 +988,12 @@ DWORD FASTCALL Memory::ReadOnly(DWORD addr) const
 	ASSERT(addr <= 0xffffff);
 	ASSERT(mem.now != None);
 
-	// ???C??RAM
+	// IPL ROM shadowed at 0x000000 during boot (AreaSet == 0)
+	if (addr < 0x20000 && areaset && areaset->GetArea() == 0) {
+		return (DWORD)mem.ipl[addr ^ 1];
+	}
+
+	// ??????????????????RAM
 	if (addr < mem.length) {
 		return (DWORD)mem.ram[addr ^ 1];
 	}
@@ -983,30 +1005,30 @@ DWORD FASTCALL Memory::ReadOnly(DWORD addr) const
 		return (DWORD)mem.ipl[addr];
 	}
 
-	// IPL?C???[?W or SCSI????
+	// IPL???????????????????????? or SCSI?????????
 	if (addr >= 0xfc0000) {
-		// IPL?C???[?W??
+		// IPL??????????????????????????????
 		if ((mem.now == SASI) || (mem.now == SCSIExt)) {
-			// IPL?C???[?W
+			// IPL????????????????????????
 			addr &= 0x1ffff;
 			addr ^= 1;
 			return (DWORD)mem.ipl[addr];
 		}
-		// SCSI??????(?????`?F?b?N)
+		// SCSI??????????????????????????????????????????)
 		if (addr < 0xfc2000) {
-			// SCSI????
+			// SCSI?????????
 			addr &= 0x1fff;
 			addr ^= 1;
 			return (DWORD)mem.scsi[addr];
 		}
-		// X68030 IPL?O????
+		// X68030 IPL???????????????
 		if (mem.now == X68030) {
-			// X68030 IPL?O??
+			// X68030 IPL?????????
 			addr &= 0x1ffff;
 			addr ^= 1;
 			return (DWORD)mem.scsi[addr];
 		}
-		// SCSI???????f?????AROM?????O
+		// SCSI???????????????????????????????????OM???????????????
 		return 0xff;
 	}
 
@@ -1017,7 +1039,7 @@ DWORD FASTCALL Memory::ReadOnly(DWORD addr) const
 		return (DWORD)mem.cg[addr];
 	}
 
-	// SCSI?O?t
+	// SCSI????????????
 	if (mem.now == SCSIExt) {
 		if ((addr >= 0xea0020) && (addr <= 0xea1fff)) {
 			addr &= 0x1fff;
@@ -1026,7 +1048,7 @@ DWORD FASTCALL Memory::ReadOnly(DWORD addr) const
 		}
 	}
 
-	// ?f?o?C?X?f?B?X?p?b?`
+	// ???????????????????????????????????????????????????
 	if (addr >= 0xc00000) {
 		index = addr - 0xc00000;
 		index >>= 13;
@@ -1036,282 +1058,35 @@ DWORD FASTCALL Memory::ReadOnly(DWORD addr) const
 		}
 	}
 
-	// ?}?b?v????????????
+	// ??????????????????????????????????????????
 	return 0xff;
 }
 
 //---------------------------------------------------------------------------
 //
-//	?R???e?L?X?g????
+//	??????????????????????????????????????????
 //
 //---------------------------------------------------------------------------
 void FASTCALL Memory::MakeContext(BOOL reset)
 {
-	int index;
-	int area;
-	GVRAM *gvram;
-	TVRAM *tvram;
-
 	ASSERT(this);
 
-	// ???Z?b?g??
+	// On reset, reset the AreaSet (called from CPU::Reset)
 	if (reset) {
-		// ?G???A?Z?b?g?????Z?b?g(CPU::Reset????MakeContext??????????????)
 		ASSERT(areaset);
 		areaset->Reset();
-
-		// ???Z?b?g???p?R???e?L?X?g($FF00000?`???A$0000000?`????????)
-		s_pgr[0].lowaddr = 0;
-		s_pgr[0].highaddr = 0xffff;
-		s_pgr[0].offset = ((DWORD)mem.ipl) + 0x10000;
-		u_pgr[0].lowaddr = 0;
-		u_pgr[0].highaddr = 0xffff;
-		u_pgr[0].offset = ((DWORD)mem.ipl) + 0x10000;
-
-		// ?v???O?????I??
-		TerminateProgramRegion(1, s_pgr);
-		TerminateProgramRegion(1, u_pgr);
-
-		// ?f?[?^???S??????
-		TerminateDataRegion(0, u_rbr);
-		TerminateDataRegion(0, s_rbr);
-		TerminateDataRegion(0, u_rwr);
-		TerminateDataRegion(0, s_rwr);
-		TerminateDataRegion(0, u_wbr);
-		TerminateDataRegion(0, s_wbr);
-		TerminateDataRegion(0, u_wwr);
-		TerminateDataRegion(0, s_wwr);
 		return;
 	}
 
-	// ?????R???e?L?X?g - ?v???O????(User)
-	index = 0;
-	area = areaset->GetArea();
-	u_pgr[index].lowaddr = (area + 1) << 13;
-	u_pgr[index].highaddr = mem.length - 1;
-	u_pgr[index].offset = (DWORD)mem.ram;
-	index++;
-	TerminateProgramRegion(index, u_pgr);
+	// NOTE: Musashi uses m68k_read/write_memory callbacks instead of
+	// Starscream memory region tables. Memory dispatch is handled by
+	// Memory::ReadByte/ReadWord/WriteByte/WriteWord which already
+	// implement the full address decoding logic.
+	// The old Starscream region table construction code has been removed.
 
-	// ?????R???e?L?X?g - ?v???O????(Super)
-	index = 0;
-	s_pgr[index].lowaddr = 0;
-	s_pgr[index].highaddr = mem.length - 1;
-	s_pgr[index].offset = (DWORD)mem.ram;
-	index++;
-
-	// IPL
-	s_pgr[index].lowaddr = 0xfe0000;
-	s_pgr[index].highaddr = 0xffffff;
-	s_pgr[index].offset = ((DWORD)mem.ipl) - 0xfe0000;
-	index++;
-
-	// SCSI?O?t
-	if (mem.now == SCSIExt) {
-		s_pgr[index].lowaddr = 0xea0000;
-		s_pgr[index].highaddr = 0xea1fff;
-		s_pgr[index].offset = ((DWORD)mem.scsi) - 0xea0000;
-		index++;
-	}
-
-	// IPL?C???[?W or SCSI????
-	if ((mem.now == SASI) || (mem.now == SCSIExt)) {
-		// IPL?C???[?W
-		s_pgr[index].lowaddr = 0xfc0000;
-		s_pgr[index].highaddr = 0xfdffff;
-		s_pgr[index].offset = ((DWORD)mem.ipl) - 0xfc0000;
-		index++;
-	}
-	else {
-		// SCSI????
-		s_pgr[index].lowaddr = 0xfc0000;
-		s_pgr[index].highaddr = 0xfc1fff;
-		s_pgr[index].offset = ((DWORD)mem.scsi) - 0xfc0000;
-		if (mem.now == X68030) {
-			// X68030 IPL?O??
-			s_pgr[index].lowaddr = 0xfc0000;
-			s_pgr[index].highaddr = 0xfdffff;
-			s_pgr[index].offset = ((DWORD)mem.scsi) - 0xfc0000;
-		}
-		index++;
-	}
-
-	// ?O???t?B?b?NVRAM
-	gvram = (GVRAM*)vm->SearchDevice(MAKEID('G', 'V', 'R', 'M'));
-	ASSERT(gvram);
-	s_pgr[index].lowaddr = 0xc00000;
-	s_pgr[index].highaddr = 0xdfffff;
-	s_pgr[index].offset = ((DWORD)gvram->GetGVRAM()) - 0xc00000;
-	index++;
-
-	// ?e?L?X?gVRAM
-	tvram = (TVRAM*)vm->SearchDevice(MAKEID('T', 'V', 'R', 'M'));
-	ASSERT(tvram);
-	s_pgr[index].lowaddr = 0xe00000;
-	s_pgr[index].highaddr = 0xe7ffff;
-	s_pgr[index].offset = ((DWORD)tvram->GetTVRAM()) - 0xe00000;
-	index++;
-
-	// SRAM
-	ASSERT(sram);
-	s_pgr[index].lowaddr = 0xed0000;
-	s_pgr[index].highaddr = 0xed0000 + (sram->GetSize() << 10) - 1;
-	s_pgr[index].offset = ((DWORD)sram->GetSRAM()) - 0xed0000;
-	index++;
-	TerminateProgramRegion(index, s_pgr);
-
-	// ?????R???e?L?X?g - ?????o??(User)
-	index = 0;
-	area = areaset->GetArea();
-
-	// ???[?U?A?N?Z?X???\????
-	u_rbr[index].lowaddr = (area + 1) << 13;
-	u_rbr[index].highaddr = mem.length - 1;
-	u_rbr[index].memorycall = NULL;
-	u_rbr[index].userdata = (void*)&mem.ram[(area + 1) << 13];
-	index++;
-
-	// ?X?[?p?o?C?U????
-	u_rbr[index].lowaddr = 0;
-	u_rbr[index].highaddr = ((area + 1) << 13) - 1;
-	u_rbr[index].memorycall = ::ReadErrC;
-	u_rbr[index].userdata = NULL;
-	index++;
-
-	// ???C???????????????????{?X?[?p?[?o?C?UI/O????
-	u_rbr[index].lowaddr = (mem.size << 20);
-	u_rbr[index].highaddr = 0xebffff;
-	u_rbr[index].memorycall = ::ReadErrC;
-	s_rbr[index].userdata = NULL;
-	index++;
-
-	// ???[?UI/O????($EC0000-$ECFFFF)
-	u_rbr[index].lowaddr = 0xec0000;
-	u_rbr[index].highaddr = 0xecffff;
-	u_rbr[index].memorycall = ::ReadByteC;
-	s_rbr[index].userdata = NULL;
-	index++;
-
-	// ?X?[?p?o?C?U????(SRAM,CG,IPL,SCSI)
-	u_rbr[index].lowaddr = 0xed0000;
-	u_rbr[index].highaddr = 0xffffff;
-	u_rbr[index].memorycall = ReadErrC;
-	s_rbr[index].userdata = NULL;
-	index++;
-	TerminateDataRegion(index, u_rbr);
-
-	// ????????(ReadWord, User)
-	memcpy(u_rwr, u_rbr, sizeof(u_rbr));
-	u_rwr[index - 2].memorycall = ::ReadWordC;
-
-	// ????????(WriteByte, User)
-	memcpy(u_wbr, u_rbr, sizeof(u_rbr));
-	u_wbr[index - 2].memorycall = ::WriteByteC;
-	u_wbr[index - 1].memorycall = ::WriteErrC;
-	u_wbr[index - 3].memorycall = ::WriteErrC;
-	u_wbr[index - 4].memorycall = ::WriteErrC;
-
-	// ????????(WriteWord, User)
-	memcpy(u_wwr, u_wbr, sizeof(u_wbr));
-	u_wwr[index - 2].memorycall = ::WriteWordC;
-
-	// ?????R???e?L?X?g - ?????o??(Super)
-	index = 0;
-	s_rbr[index].lowaddr = 0;
-	s_rbr[index].highaddr = mem.length - 1;
-	s_rbr[index].memorycall = NULL;
-	s_rbr[index].userdata = (void*)mem.ram;
-	index++;
-
-	// CG
-	s_rbr[index].lowaddr = 0xf00000;
-	s_rbr[index].highaddr = 0xfbffff;
-	s_rbr[index].memorycall = NULL;
-	s_rbr[index].userdata = (void*)mem.cg;
-	index++;
-
-	// IPL
-	s_rbr[index].lowaddr = 0xfe0000;
-	s_rbr[index].highaddr = 0xffffff;
-	s_rbr[index].memorycall = NULL;
-	s_rbr[index].userdata = (void*)mem.ipl;
-	index++;
-
-	// SCSI?O?t
-	if (mem.now == SCSIExt) {
-		s_rbr[index].lowaddr = 0xea0020;
-		s_rbr[index].highaddr = 0xea1fff;
-		s_rbr[index].memorycall = NULL;
-		s_rbr[index].userdata = (void*)(&mem.scsi[0x20]);
-		index++;
-	}
-
-	// IPL?C???[?W or SCSI????
-	if ((mem.now == SASI) || (mem.now == SCSIExt)) {
-		// IPL?C???[?W
-		s_rbr[index].lowaddr = 0xfc0000;
-		s_rbr[index].highaddr = 0xfdffff;
-		s_rbr[index].memorycall = NULL;
-		s_rbr[index].userdata = (void*)mem.ipl;
-		index++;
-	}
-	else {
-		// SCSI????
-		s_rbr[index].lowaddr = 0xfc0000;
-		s_rbr[index].highaddr = 0xfc1fff;
-		s_rbr[index].memorycall = NULL;
-		s_rbr[index].userdata = (void*)mem.scsi;
-		if (mem.now == X68030) {
-			// X68030 IPL?O??
-			s_rbr[index].lowaddr = 0xfc0000;
-			s_rbr[index].highaddr = 0xfdffff;
-			s_rbr[index].memorycall = NULL;
-			s_rbr[index].userdata = (void*)mem.scsi;
-		}
-		index++;
-	}
-
-	// ???????O(?O???R?[??)
-	s_rbr[index].lowaddr = (mem.size << 20);
-	s_rbr[index].highaddr = 0xefffff;
-	s_rbr[index].memorycall = ::ReadByteC;
-	s_rbr[index].userdata = NULL;
-	index++;
-	TerminateDataRegion(index, s_rbr);
-
-	// ????????
-	memcpy(s_rwr, s_rbr, sizeof(s_rbr));
-	s_rwr[index - 1].memorycall = ::ReadWordC;
-
-	// ?????R???e?L?X?g - ????????(Super)
-	index = 0;
-	s_wbr[index].lowaddr = 0;
-	s_wbr[index].highaddr = mem.length - 1;
-	s_wbr[index].memorycall = NULL;
-	s_wbr[index].userdata = (void*)mem.ram;
-	index++;
-
-	// ???????O(?O???R?[??)
-	s_wbr[index].lowaddr = (mem.size << 20);
-	s_wbr[index].highaddr = 0xefffff;
-	s_wbr[index].memorycall = ::WriteByteC;
-	s_wbr[index].userdata = NULL;
-	index++;
-	TerminateDataRegion(index, s_wbr);
-
-	// ????????
-	memcpy(s_wwr, s_wbr, sizeof(s_wbr));
-	s_wwr[index - 1].memorycall = ::WriteWordC;
-
-	// cpu->Release???Y??????
+	// Force CPU to exit current instruction (was at the end of old MakeContext)
 	cpu->Release();
 }
-
-//---------------------------------------------------------------------------
-//
-//	?v???O???????[?W?????I??
-//
-//---------------------------------------------------------------------------
 void FASTCALL Memory::TerminateProgramRegion(int index, STARSCREAM_PROGRAMREGION *spr)
 {
 	ASSERT(this);
@@ -1325,7 +1100,7 @@ void FASTCALL Memory::TerminateProgramRegion(int index, STARSCREAM_PROGRAMREGION
 
 //---------------------------------------------------------------------------
 //
-//	?f?[?^???[?W?????I??
+//	?????????????????????????????????????????????????????????
 //
 //---------------------------------------------------------------------------
 void FASTCALL Memory::TerminateDataRegion(int index, STARSCREAM_DATAREGION *sdr)
@@ -1342,8 +1117,8 @@ void FASTCALL Memory::TerminateDataRegion(int index, STARSCREAM_DATAREGION *sdr)
 
 //---------------------------------------------------------------------------
 //
-//	IPL?o?[?W?????`?F?b?N
-//	??IPL??version1.00(87/05/07)???????????????`?F?b?N
+//	IPL?????????????????????????????????????????????
+//	??????IPL??????ersion1.00(87/05/07)???????????????????????????????????????????????????
 //
 //---------------------------------------------------------------------------
 BOOL FASTCALL Memory::CheckIPL() const
@@ -1351,17 +1126,17 @@ BOOL FASTCALL Memory::CheckIPL() const
 	ASSERT(this);
 	ASSERT(mem.now != None);
 
-	// ?????`?F?b?N
+	// ???????????????????????????
 	if (!mem.ipl) {
 		return FALSE;
 	}
 
-	// SASI?^?C?v???????????`?F?b?N????
+	// SASI?????????????????????????????????????????????????????????????????????
 	if (mem.now != SASI) {
 		return TRUE;
 	}
 
-	// ???t(BCD)???`?F?b?N
+	// ????????????BCD)????????????????????????
 	if (mem.ipl[0x1000a] != 0x87) {
 		return FALSE;
 	}
@@ -1377,8 +1152,8 @@ BOOL FASTCALL Memory::CheckIPL() const
 
 //---------------------------------------------------------------------------
 //
-//	CG?`?F?b?N
-//	??8x8?h?b?g?t?H???g(?S?@??????)??Sum,Xor???`?F?b?N
+//	CG??????????????????
+//	??????8x8????????????????????????????????????????????????????????????????????????Sum,Xor????????????????????????
 //
 //---------------------------------------------------------------------------
 BOOL FASTCALL Memory::CheckCG() const
@@ -1391,24 +1166,24 @@ BOOL FASTCALL Memory::CheckCG() const
 	ASSERT(this);
 	ASSERT(mem.now != None);
 
-	// ?????`?F?b?N
+	// ???????????????????????????
 	if (!mem.cg) {
 		return FALSE;
 	}
 
-	// ????????
+	// ?????????????????????
 	add = 0;
 	eor = 0;
 	ptr = &mem.cg[0x3a800];
 
-	// ADD, XOR???[?v
+	// ADD, XOR??????????????????
 	for (i=0; i<0x1000; i++) {
 		add = (BYTE)(add + *ptr);
 		eor ^= *ptr;
 		ptr++;
 	}
 
-	// ?`?F?b?N(XVI?????????l)
+	// ??????????????????(XVI??????????????????????????????)
 	if ((add != 0xec) || (eor != 0x84)) {
 		return FALSE;
 	}
@@ -1418,7 +1193,7 @@ BOOL FASTCALL Memory::CheckCG() const
 
 //---------------------------------------------------------------------------
 //
-//	CG????
+//	CG????????????
 //
 //---------------------------------------------------------------------------
 const BYTE* FASTCALL Memory::GetCG() const
@@ -1431,7 +1206,7 @@ const BYTE* FASTCALL Memory::GetCG() const
 
 //---------------------------------------------------------------------------
 //
-//	SCSI????
+//	SCSI????????????
 //
 //---------------------------------------------------------------------------
 const BYTE* FASTCALL Memory::GetSCSI() const
@@ -1441,3 +1216,37 @@ const BYTE* FASTCALL Memory::GetSCSI() const
 
 	return mem.scsi;
 }
+
+//---------------------------------------------------------------------------
+//
+//	Musashi Memory Bridge Functions
+//	Called from musashi_mem.cpp -> m68k_read/write_memory_*
+//
+//---------------------------------------------------------------------------
+extern "C" {
+
+DWORD FASTCALL MusashiReadByte(DWORD addr)
+{
+	ASSERT(pMemory);
+	return pMemory->ReadByte(addr);
+}
+
+DWORD FASTCALL MusashiReadWord(DWORD addr)
+{
+	ASSERT(pMemory);
+	return pMemory->ReadWord(addr);
+}
+
+void FASTCALL MusashiWriteByte(DWORD addr, DWORD data)
+{
+	ASSERT(pMemory);
+	pMemory->WriteByte(addr, data);
+}
+
+void FASTCALL MusashiWriteWord(DWORD addr, DWORD data)
+{
+	ASSERT(pMemory);
+	pMemory->WriteWord(addr, data);
+}
+
+} // extern "C"
